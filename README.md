# Test Repo

A dummy repository for testing git GUI clients.

## Structure

- `app.py` — tiny Flask-style web app
- `utils.js` — JavaScript utility helpers
- `config.json` — sample configuration
- `data/` — sample data files

## Usage

```bash
python app.py
```
